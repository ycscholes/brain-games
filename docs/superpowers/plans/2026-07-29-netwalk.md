# 网络回路 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add the `netwalk` mini-game with deterministic board logic, shared rewards, and gauntlet integration.

**Architecture:** Keep all puzzle rules, template definitions, rotation, hinting, solved validation, and scoring in a pure page-local `gameLogic.ts` module. The page owns UI phase/timer state and uses the existing shared game completion pipeline; shared registries only describe the new playable game.

**Tech Stack:** React 18, Taro 4, TypeScript, SCSS, Jest.

---

### Task 1: Define test-first Netwalk puzzle logic

**Files:**
- Create: `tests/unit/netwalkGameLogic.test.ts`
- Create: `src/pages/netwalk/gameLogic.ts`

- [x] Write a failing test that iterates every `NETWALK_TEMPLATES` item, verifies its solution state solves, its rotated initial state does not solve, and checks rotation, hints, reciprocal interfaces, boundary rejection, BFS reachability, and the normal/hard 40/50 score caps.
- [x] Run `npm test -- tests/unit/netwalkGameLogic.test.ts --runInBand` and confirm it fails because the module is missing.
- [x] Implement typed directions, static transformed tree templates (three each per difficulty), immutable clockwise rotation, one-step hint, solved validation, and score calculation.
- [x] Re-run the focused test and confirm it passes.

### Task 2: Register the game in shared systems

**Files:**
- Modify: `src/app.config.ts`
- Modify: `src/config/gameCatalog.ts`
- Modify: `src/utils/share.ts`
- Modify: `src/utils/trainingStorage.ts`
- Modify: `tests/unit/gameCatalog.test.ts`
- Modify: `tests/unit/gameGauntlet.test.ts`
- Modify: `tests/unit/trainingStorage.test.ts`

- [x] Add failing assertions that `netwalk` appears in all-games and gauntlet candidates and maps `40` normal / `50` hard game score to `40` / `60` pet points.
- [x] Run `npm test -- tests/unit/gameCatalog.test.ts tests/unit/gameGauntlet.test.ts tests/unit/trainingStorage.test.ts --runInBand` and confirm failure.
- [x] Register `netwalk` in `TrainingGameId`, the 1.0 reward conversion/cleanup keys, `推理` catalogue category, route/share map, and gauntlet candidate list. Use `url: "/pages/netwalk/index"`, `cardClass: "card-netwalk"`, one gauntlet mode weight, and no homepage-hot override.
- [x] Re-run the three registry suites and confirm they pass.

### Task 3: Build the mobile game page and styling

**Files:**
- Create: `src/pages/netwalk/index.tsx`
- Create: `src/pages/netwalk/index.scss`
- Create: `src/pages/netwalk/index.config.ts`
- Modify: `src/styles/game-list.scss`

- [x] Build `start`, `playing`, and `finished` views. Start chooses normal/hard unless launched by gauntlet; playing renders CSS connector arms from `NetwalkState`, timer/move/hint HUD, and click-to-rotate nodes; finished displays score, points, and replay actions.
- [x] At completion call `getAwardedPoints("netwalk", score, difficulty)` then `completeGauntletLegIfNeeded`. Only normal runs call `addPointsToPet`, `recordTrainingSession`, and update `netwalk_best_${difficulty}`.
- [x] Use the deep-sea control-room visual system: navy panels, amber server core, cyan connected lines, muted disconnected lines, a 90-degree rotation transition, and a high-contrast reasoning-category card. Keep controls 44px+ and do not make state color-only.

### Task 4: Document and verify the feature

**Files:**
- Modify: `docs/points-economy.md`
- Modify: `docs/game-candidate-pool.md`

- [x] Add the 1.0x normal/hard reward row, `netwalk` ID, and expected score ranges to the points economy document.
- [x] Run focused Jest, full Jest, typecheck, lint, WeChat build, secrets check, and `git diff --check`.
- [ ] Commit only the Netwalk feature, docs, and tests using `feat: add netwalk mini-game`.
